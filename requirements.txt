# Solace Python Backend Dependencies
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# OpenAI (for AI agents)
openai>=1.12.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client (async)
httpx>=0.26.0

# Pydantic (data validation, included with FastAPI but pinned for compatibility)
pydantic>=2.5.0
